name: "🙋 Pull Request"

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  development:
    name: 👷 Development
    # TODO: ❗️ Remove this before merging!
    uses: remix-austin/remixaustin-com/.github/workflows/development.yml@gkn/update-node-to-hydrogen
